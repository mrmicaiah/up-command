{
  "name": "up-command-server",
  "version": "1.0.0",
  "description": "UP Command API Server",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:irene": "wrangler deploy --config wrangler-irene.jsonc",
    "deploy:all": "npm run deploy && npm run deploy:irene",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240512.0",
    "typescript": "^5.4.5",
    "wrangler": "^3.57.0"
  }
}